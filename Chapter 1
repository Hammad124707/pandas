
Pandas ki data frame mein 3 zaroori cheezien hoti hein. We call them 3 components.
a)Index (axis = 0)
b)Columns (axis = 1)
c) Data

Each of this component can be extracted from the data frame using their own  *attribute.

columns = df.columns
index = df.index

data = df.to_numpy()  #this method converts data into a numpy array of respective data type, haterogenious data gets converted into object dtype.
I can also convert index data type into numpy array data type using index.to_numpy() but rn i have to idea whats the use of it and is there any advantage or not.




NaN means missing value, idhr value daali nhi gyi.
